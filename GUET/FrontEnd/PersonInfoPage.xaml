<Page
    x:Class="GUET.FrontEnd.PersonInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUET.FrontEnd"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--<ComboBox ItemsSource="{x:Bind terms}" Width="100" HorizontalAlignment="Stretch"/>
            <ComboBox ItemsSource="{x:Bind schoolWeeks}" Width="100" HorizontalAlignment="Stretch"/>-->
            <TextBlock x:Name="TextBlock_Name"/>
            <TextBlock x:Name="TextBlock_grade"/>
            <HyperlinkButton Content="退出登录" Click="HyperlinkButton_Click_Logout"/>
        </StackPanel>
    </Grid>
</Page>
