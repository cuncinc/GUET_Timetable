<Page
    x:Class="GUET.FrontEnd.PersonInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUET.FrontEnd"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                <ComboBox x:Name="ComboBox_term" ItemsSource="{x:Bind terms}" Width="100" HorizontalAlignment="Stretch" Header="课表学期" Margin="0,0,24,0" SelectionChanged="ComboBox_SelectionChanged_Term"/>
                <ComboBox x:Name="ComboBox_week" ItemsSource="{x:Bind schoolWeeks}" Width="100" HorizontalAlignment="Stretch" Header="当前周次" SelectionChanged="ComboBox_SelectionChanged_Week"/>
            </StackPanel>
            <TextBlock x:Name="TextBlock_Name" HorizontalAlignment="Center"/>
            <TextBlock x:Name="TextBlock_grade" HorizontalAlignment="Center"/>
            <HyperlinkButton Content="退出登录" Click="HyperlinkButton_Click_Logout" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
